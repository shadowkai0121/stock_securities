# Storage Layer

`sqlite_store.py` provides consistent read/query utilities for local SQLite data.

This module is used by research loaders and validation checks. It never reaches remote data sources.
