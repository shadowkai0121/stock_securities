# Data Catalog

`data_catalog.yaml` defines each supported dataset, storage mapping, ingestion command, and quality checks.

The catalog is intentionally machine-readable and human-readable so orchestrators and agents can enforce dataset prerequisites before running research jobs.
